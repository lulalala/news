<h2><%= title '本使用者認證過的網路身份' %></h2>
<p>
本頁面是為證明該糾正的內容來自以下 Facebook 的使用者。
</p>
<ul>
<% @user.identities.each do |identity| %>
  <li>
    <span class="label"><%= identity.provider.capitalize %></span>
    <%= link_to('頁面連結', identity.url) if identity.url %>
    <%= image_tag identity.image if identity.image %>
  </li>
<% end %>
</ul>
