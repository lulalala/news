<table class="table">
  <p><b>媒體：</b><%= @article.company_name %> <b>記者：</b><%= @article.reporter_name %></p>
  <h2><%= @article.title %></h2>
  <thead>
    <th class="content">內文</th>
    <th class="control"></th>
  </thead>
  <tbody>
<% @article.lines.each do |l| %>
  <tr>
    <td class="span6">
      <%= l.text %>
    </td>
    <td>
      <%= link_to '改正', new_review_path(line_id:l.id), class:'btn', :data => { :colorbox => true } %>
    </td>
    <% l.reviews.each do |r| %>
      <td>
        <span class="label"><%= r.tags.first.try(:name) %></span>
        <%= r.text_html %>
      </td>
    <% end %>
  </tr>
<% end %>
</tbody>
</table>

<h3>相關網頁</h3>
<p>與報導有相關但是非關事實對錯的連結，如反方報導或是深入閱讀等</p>
<table class='table'>
<% @article.links.each do |article| %>
  <tr>
    <td><%= article.url %></td>
    <td><%= article.description %></td>
  </tr>
<% end %>
</table>
<%= link_to '新增', new_article_link_path(@article), :data => { :colorbox => true }, class:'btn' %>
